<!--絞り込み機能未実装-->

    <div class="col-md-3 text-center">
      <table class="table table-sm table-hover table-borderless border border-dark ">
        <div class="d-flex align-items-end" style="height:15px;">
          
          <tr class="border border-dark">
            <th class="table-info">ジャンル検索</th>
          </tr>
          
          <%= form_with url: search_path, method: :get do |f| %>
            <% @genres.each do |genre| %>
              <tr>
                <td><%= link_to genre.name, items_path(genre_id: genre), method: :get %></td>
              </tr>
            <% end %>
          <% end %>
          
        </div>
      </table>
    </div>
